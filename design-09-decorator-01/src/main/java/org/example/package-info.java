/**
 * @description:装饰器模式
 * 装饰器的核⼼就是再不改原有类的基础上 给类新增功能。不改变原有类，
 * 可能有的⼩伙伴会想到继承、AOP切⾯，当然这些⽅式都可以实现，
 * 但是使⽤装饰器模式会是另外⼀种思路更为灵活，可以避免继承导致的⼦类过多，也可以避免AOP带来的 复杂性。
 * @author：wwei
 * @date: 2022/12/26
 */
package org.example;